#include "lattice.h"

using namespace std;
Lattice::Lattice(ElementSequence element_sequence, string name)
  : sequence_(element_sequence), length_(0){

  for(vector)
  // checking for RF elements and filling out rf_metadata_
  
}
